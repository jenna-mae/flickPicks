import styled from 'styled-components';

const DetailText = styled.Text`
  font-style: normal;
  font-weight: normal;
  font-size: 12px;
  line-height: 14px;
  margin: 20px 50px;

  color: #ffffff;
`;

export default DetailText;
