import styled from 'styled-components';

const SearchTitle = styled.Text`
  color: white;
  font-size: 22px;
  font-weight: bold;
  padding-top: 40px;
  padding-bottom: 20px;
  padding-left: 14px;
  text-align: center;
`;

export default SearchTitle;
